spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fourcylinder?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: q1w2e3r4!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  mvc:
    encoding:
      charset: UTF-8
      force: true
