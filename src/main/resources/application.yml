server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fourcylinder?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 1234!
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mappers/**/*.xml
  type-aliases-package: com.example.FourCylinder.domain

logging:
  level:
    org.apache.ibatis: debug
    com.example.FourCylinder: debug
